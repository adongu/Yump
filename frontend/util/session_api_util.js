const signup (user) => {
  method:"POST",
  url: "api/users",
  data: { user }
}

const signup (user) => {
  method:"POST",
  url: "api/users",
  data: { user }
}

const signup () => {
  method:"DELETE",
  url: "api/users",
}
